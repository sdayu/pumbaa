{% extends "/base/default-dashboard.jinja2" %}

{% block breadcrumbs %}
  <a class="breadcrumb" href="{{ url_for('dashboard.topics.index') }}">Topics</a>
{% endblock %}

{% block content %}
  <div class="row">
    <div class="col s12">
    <h3>My Topics</h3>
    <a href="{{ url_for('dashboard.topics.compose') }}">new topic</a>
</ul>
<ul class="collection">
  {% for topic in topics %}
   <li class="collection-item">
     <a href="{{ url_for('forums.topics.view', title=topic.title, topic_id=topic.id)}}">{{ topic.title }}</a>
    </li>
    {% endfor %}
</ul>
    </div>
  </div>
{% endblock %}
